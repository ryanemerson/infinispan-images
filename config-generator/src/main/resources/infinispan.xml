<infinispan
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="urn:infinispan:config:10.0 http://www.infinispan.org/schemas/infinispan-config-10.0.xsd
                            urn:infinispan:server:10.0 http://www.infinispan.org/schemas/infinispan-server-10.0.xsd"
        xmlns="urn:infinispan:config:10.0"
        xmlns:server="urn:infinispan:server:10.0"
        xmlns:gsp='http://groovy.codehaus.org/2005/gsp'>

    <jgroups>
        <stack-file name="image-${jgroups.transport}" path="jgroups-${jgroups.transport}.xml"/>
    </jgroups>

    <cache-container>
        <transport cluster="${infinispan.clusterName}" stack="image-${jgroups.transport}"/>
    </cache-container>

    <server xmlns="urn:infinispan:server:10.0">
        <interfaces>
            <interface name="public">
                <inet-address value="${bindAddress}"/>
            </interface>
        </interfaces>

        <socket-bindings default-interface="public" port-offset="0">
            <socket-binding name="default" port="11222"/>
        </socket-bindings>

        <security>
            <security-realms>
                <security-realm name="default">
                    <properties-realm groups-attribute="Roles">
                        <user-properties path="users.properties" relative-to="infinispan.server.config.path"
                                         plain-text="true"/>
                        <group-properties path="groups.properties" relative-to="infinispan.server.config.path"/>
                    </properties-realm>

                    <gsp:scriptlet>
                        if ("${keystore.path}" != "null") {
                    </gsp:scriptlet>
                    <server-identities>
                        <ssl>
                            <keystore path="${keystore.path}" keystore-password="${keystore.password}"/>
                        </ssl>
                    </server-identities>
                    <gsp:scriptlet>
                        }
                    </gsp:scriptlet>
                </security-realm>
            </security-realms>
        </security>

        <endpoints socket-binding="default" security-realm="default">
            <hotrod-connector name="hotrod" >
                <authentication security-realm="default">
                    <sasl mechanisms="DIGEST-MD5"/>
                </authentication>
            </hotrod-connector>
            <rest-connector name="rest">
                <authentication mechanisms="DIGEST BASIC" security-realm="default"/>
            </rest-connector>
        </endpoints>
    </server>
</infinispan>